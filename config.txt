# Настройки reverse proxy.
# Формат: KEY=VALUE, по одному параметру на строку. Пустые строки и строки,
# начинающиеся с #, игнорируются; значения можно брать в кавычки.
# Приоритет: переменные окружения > этот файл > значения по умолчанию.

# Целевой сайт (по умолчанию https://ru.wikipedia.org)
# TARGET_URL=https://ru.wikipedia.org

# Доп. домены (CDN/SSO) через запятую; доступны как /__<домен>__/<путь>.
# Пусто — доп. доменов нет.
# EXTRA_TARGET_DOMAINS=

# Хост и порт, на которых слушает прокси
# PROXY_HOST=127.0.0.1
# PROXY_PORT=8000

# Тела ответов больше этого размера (байт) отдаются без URL-rewriting
# MAX_REWRITE_BODY_SIZE=8388608

# Подробные логи: 1 / true / yes
# DEBUG=

# Базовый путь (префикс JupyterHub), например:
BASE_PATH=/user/my_login/proxy/8000
# Можно указать и полный URL — https://jupyterhub.example/user/my_login/proxy/8000,
# будет взят только путь. Пусто — обычный http://localhost:PORT/ без префикса.
# ВАЖНО: префикс должен совпадать с путём в nginx.conf (имитация JupyterHub).
# BASE_PATH=
